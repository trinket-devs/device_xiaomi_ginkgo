allow vendor_hal_perf_default init:dir { read search open };
allow vendor_hal_perf_default kernel:dir { read search open };
allow vendor_hal_perf_default system_server:dir search;
allow vendor_hal_perf_default surfaceflinger:dir search;
allow vendor_hal_perf_default system_server:file { read getattr open };
allow vendor_hal_perf_default system_server:dir { read search open };
